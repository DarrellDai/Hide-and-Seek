using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class Test : MonoBehaviour
{
    private TerrainAndRockSetting terrainAndRockSetting1;
    private TerrainAndRockSetting terrainAndRockSetting2;
    private TerrainAndRockSetting terrainAndRockSetting3;
    private void Awake()
    {
    }

    // Start is called before the first frame update 
    void Start()
    {
    } 

    // Update is called once per frame
    void Update() 
    {
        //GameObject.Find("Destination").transform.position += Vector3.down * 0.01f;
        //transform1.position+= Vector3.down * 0.01f;
        //transform.position += Vector3.down*0.01f; 
        //Debug.Log(transform.position);
        //Debug.Log(GameObject.Find("Destination").transform.position);
    }
}
